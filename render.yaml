services:
  - type: web
    name: brain-tumor-detection
    runtime: python
    buildCommand: pip install -r requirements-deploy.txt
    startCommand: gunicorn --chdir flask_app -w 2 -b 0.0.0.0:$PORT app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
